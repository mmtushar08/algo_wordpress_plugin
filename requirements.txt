# ── Core data & analysis ──────────────────────────────────────────────────────
pandas>=1.5
numpy>=1.23
yfinance>=0.2          # historical data (backtesting / fallback)

# ── Angel One SmartAPI (live data + order placement) ─────────────────────────
smartapi-python>=1.3   # pip install smartapi-python
pyotp>=2.8             # TOTP 2FA token generation
websocket-client>=1.6  # required by SmartWebSocketV2

# ── Configuration ─────────────────────────────────────────────────────────────
python-dotenv>=1.0     # loads .env file into os.environ

# ── Alerts ────────────────────────────────────────────────────────────────────
requests>=2.28         # Telegram Bot API calls

# ── Timezone ──────────────────────────────────────────────────────────────────
pytz>=2023.3           # IST market hours check
