# ── Core data & analysis ──────────────────────────────────────────────────────
pandas>=1.5
numpy>=1.23
yfinance>=0.2          # historical OHLCV candles (daily + intraday)

# ── Live NSE data (no broker account needed) ──────────────────────────────────
requests>=2.28         # NSE India web API + Telegram Bot API

# ── Configuration ─────────────────────────────────────────────────────────────
python-dotenv>=1.0     # loads .env into os.environ

# ── Timezone ──────────────────────────────────────────────────────────────────
pytz>=2023.3           # IST market hours check

# ── Optional broker SDKs for live order placement ─────────────────────────────
# Uncomment whichever broker you use — only one needed:
# kiteconnect>=5.0       # Zerodha Kite Connect  (₹2,000/mo fee)
# upstox-python-sdk      # Upstox  (free with account)
# fyers-apiv3            # Fyers   (free with account)
# breeze-connect         # ICICI Direct Breeze  (free with account)
